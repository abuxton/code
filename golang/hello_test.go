//Package Hello test
package hello

import "testing"

//TestHello function to test hello
func TestHello(t *testing.T) {
	want := "Hello, world."
	if got := Hello(); got != want {
		t.Errorf("Hello() = %q, want %q", got, want)
	}
}
